<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        개발 환경: 모든 도메인에 대해치 cleartext(HTTP) 허용

        주의: 프로덕션 환경에서는 HTTPS를 사용하거나
        특정 도메인만 허용하도록 설정해야 합니다.

        서버 IP는 local.properties의 server.host에서 관리됩니다.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
