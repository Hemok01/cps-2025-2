🎉 테스트 계획 완료!

  📋 테스트 결과 요약

  ✅ 성공한 테스트:

  1. 백엔드 서버 - 정상 실행 중
  (http://localhost:8000)
  2. 인증 시스템 - 회원가입, 로그인, JWT
  토큰 발급 정상
  3. 테스트 데이터 생성:
    - Lecture ID: 3 (스마트폰 기초
  사용법)
    - Task ID: 3 (카카오톡 설치하기)
    - Subtasks: 4, 5, 6 (플레이스토어
  열기, 카카오톡 검색, 설치)
    - Session ID: 5 (Code: NUCP8M)
  4. Activity Log - 로그 전송 및 저장
  정상
  5. WebSocket - 단계 이동, 일시정지,
  재개, 종료 모두 정상

  ⚠️ 발견된 버그:

  - /api/progress/me/ 엔드포인트에
  QuerySet 슬라이싱 문제 (backend/apps/pr
  ogress/views.py:MyProgressView)

  ---
  🚀 안드로이드 앱에서 실제 테스트하는 
  방법

  필수 정보:

  📱 학생 계정:
  Email: student1@test.com
  Password: TestStudent123!@#

  🔑 세션 코드: NUCP8M

  🌐 API URL (앱 설정):
  BASE_URL = http://10.0.2.2:8000/api/
  WS_URL = ws://10.0.2.2:8000/ws/

  테스트 순서:

  1단계: 접근성 서비스 활성화
  Android 설정 → 접근성 → MobileGPT
  Accessibility Service → ON

  2단계: 앱 로그인
  앱 실행 → 로그인 → student1@test.com

  3단계: 세션 참여
  세션 코드 입력: NUCP8M → 참여하기

  4단계: 이벤트 캡처 테스트
  앱 내 버튼 클릭, 스크롤 → Logcat에서
  이벤트 로그 확인
  adb logcat | grep "MobileGPT"

  5단계: 단계 이동 테스트
  # 터미널에서 다음 명령 실행 (강사 
  역할):
  curl -X POST http://localhost:8000/api/
  sessions/5/next-step/ \
    -H "Authorization: Bearer 
  <INSTRUCTOR_TOKEN>" \
    -H "Content-Type: application/json" \
    -d '{"next_subtask_id": 6}'

  → 앱에서 새로운 단계 알림 수신 확인

  ---
  🔧 디버깅 팁

  백엔드 로그 확인:

  # 서버 로그는 자동으로 표시됩니다
  # 또는 명시적으로 확인:
  cd /Users/heemok/cps\ 2025-2/backend
  python manage.py runserver 8000

  안드로이드 로그 확인:

  adb logcat | grep -E
  "MobileGPT|Accessibility|WebSocket"

  네트워크 요청 확인:

  # Logcat에서 Retrofit/OkHttp 로그 확인
  adb logcat | grep -E "OkHttp|Retrofit"

  WebSocket 연결 확인:

  # wscat으로 수동 테스트
  npm install -g wscat
  wscat -c "ws://localhost:8000/ws/sessio
  ns/NUCP8M/"
